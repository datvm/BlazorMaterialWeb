@namespace BlazorMaterialWeb
@inherits DefaultMdComponent

<md-list-item @attributes="AdditionalAttributes"
@onlistitemrequestactivation="OnRequestActivation"
              disabled="@(Disabled)"
              type="@(finalType?.ToString().ToLower())"
              href="@(Href)"
              target="@(Target)">
    @(ChildContent)
</md-list-item>