@inherits DefaultMdComponent

<md-checkbox @oncheckedchange="OnCheckedChanged" @attributes="AdditionalAttributes"
             checked="@(Checked)"
             indeterminate="@(Indeterminate)"
             disabled="@(Disabled)"
             required="@(Required)"
             value="@(Value)">
    @(ChildContent)
</md-checkbox>
