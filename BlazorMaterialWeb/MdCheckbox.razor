@inherits DefaultInputComponent

<md-checkbox @oncheckedchange="OnCheckedChanged" @attributes="AdditionalAttributes" @ref="el"
             checked="@(Checked)"
             indeterminate="@(Indeterminate)"
             disabled="@(Disabled)"
             required="@(Required)"
             value="@(Value)">
    @(ChildContent)
</md-checkbox>
