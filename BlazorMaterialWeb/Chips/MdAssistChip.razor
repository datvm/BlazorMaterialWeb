@namespace BlazorMaterialWeb

@inherits BaseMdChip

<BaseMdChipRenderer Type="MdChipType.Assist" @attributes="AdditionalAttributes"
                    href="@(Href)"
                    target="@(Target)"
                    Label="@(Label)"
                    Elevated="@(Elevated)"
                    Disabled="Disabled">
    @(ChildContent)
</BaseMdChipRenderer>