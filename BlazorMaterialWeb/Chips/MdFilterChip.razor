@namespace BlazorMaterialWeb

@inherits BaseMdChip

<BaseMdChipRenderer Type="MdChipType.Filter"
                    removable="@(Removable)"
                    selected="@(Selected)"
                    @onselected="OnSelectedChanged"
                    @onremove="OnRemove"
                    @attributes="AdditionalAttributes"
                    Label="@(Label)"
                    Elevated="@(Elevated)"
                    Disabled="Disabled">
    @(ChildContent)
</BaseMdChipRenderer>