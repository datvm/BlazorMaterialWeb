@inherits DefaultMdComponent

<md-radio @attributes="AdditionalAttributes" @ref="el"
          @oncheckedchange="OnCheckedChanged"
          checked="@(Checked)"
          disabled="@(Disabled)"
          value="@(Value)">
    @(ChildContent)
</md-radio>
