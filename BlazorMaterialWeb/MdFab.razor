@inherits DefaultMdComponent

<DynamicTag Tag="@(TagName)" @attributes="AdditionalAttributes" @bind-DynamicElementReference="el"
            variant="@(Variant.ToString().ToLower())"
            label="@(Label)"
            size="@(Size.ToString().ToLower())">
    @(ChildContent)
</DynamicTag>