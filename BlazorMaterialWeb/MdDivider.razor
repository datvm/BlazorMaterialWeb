@inherits DefaultMdComponent

<md-divider @attributes="AdditionalAttributes"
            inset="@(Inset)"
            inset-start="@(InsetStart)"
            inset-end="@(InsetEnd)">
    @(ChildContent)
</md-divider>