@inherits DefaultMdComponent

<md-divider @attributes="AdditionalAttributes" @ref="el"
            inset="@(Inset)"
            inset-start="@(InsetStart)"
            inset-end="@(InsetEnd)">
    @(ChildContent)
</md-divider>