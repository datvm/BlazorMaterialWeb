@inherits DefaultInputComponent

<md-switch @attributes="AdditionalAttributes" @ref="el"
           disabled="@(Disabled)"
           selected="@(Selected)"
           value="@(Value)"
           required="@(Required)"
           icons="@(Icons)"
           show-only-selected-icon="@(ShowOnlySelectedIcon)"
           @onswitchchange="OnSelectedChanged">
    @(ChildContent)
</md-switch>
